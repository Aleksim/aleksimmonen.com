import React from "react"

const SiteIntro = () => (
  <div className="site-intro">
    <p><span className="growth-span">Growth is always changing</span>...but there has never been as much leverage.</p>
  </div>
)

export default SiteIntro
